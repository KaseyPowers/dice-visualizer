# Digit

123
21345

==>

Program(Digit, Digit)

# Identifiers

one
Two_Three

==>

Program(Identifier, Identifier)

# Strings

"hello"
"one\"\\two"
" a longer string "
"split

==>

Program(String, String, String, String(⚠))

# Arrays 

[1,2]
[1..5,10]

==>

Program(
  ArrayExpression(Digit, Digit),
  ArrayExpression(Range(Digit, Digit), Digit)
)

# Dice

d10
d 10
d[1,2]
d(1 + 5)

==>

Program(
  Dice(Digit),
  Dice(⚠), Digit,
  Dice(ArrayExpression(Digit, Digit)),
  Dice(ParenthesizedExpression(ArithmeticExpression(Digit, Digit)))
)

# Arithmetic (basic)

1 + 1
5 - 1
2 * 3
4 / 4
4 % 3


==> 
Program(
  ArithmeticExpression(Digit, Digit), 
  ArithmeticExpression(Digit, Digit),
  ArithmeticExpression(Digit, Digit),
  ArithmeticExpression(Digit, Digit),
  ArithmeticExpression(Digit, Digit)
)

# Arithmetic (mixed)

1 + 2 * 3
1 * 4 - 5/2 + 3

==> 

Program(
  ArithmeticExpression(Digit, ArithmeticExpression(Digit, Digit))
  ArithmeticExpression(
    ArithmeticExpression(
      ArithmeticExpression(Digit, Digit),
      ArithmeticExpression(Digit, Digit)
    ), 
    Digit
  )
)